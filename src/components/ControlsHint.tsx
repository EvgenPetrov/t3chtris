export default function ControlsHint() {
    return (
        <div className="controls">
            <span>← → — движение</span>
            <span>↑ — вращение</span>
            <span>↓ — ускорить падение</span>
            <span>Space — хард-дроп</span>
            <span>P — пауза</span>
            <span>R — рестарт</span>
        </div>
    );
}
